import React from 'react';
import ReactDOM from 'react-dom/client';
import HIITIntervalTimer from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <HIITIntervalTimer />
  </React.StrictMode>
);
